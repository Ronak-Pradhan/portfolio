export interface ProjectsPageContent {
  pageTitle: string
  subtitle: string
}

const projectsPageContent: ProjectsPageContent = {
  pageTitle: 'Projects',
  subtitle: 'Things I have built.',
}

export default projectsPageContent
